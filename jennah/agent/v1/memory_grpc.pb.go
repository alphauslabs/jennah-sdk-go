// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: jennah/agent/v1/memory.proto

package agent

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	MemoryService_CommitMemory_FullMethodName  = "/jennahapi.agent.v1.MemoryService/CommitMemory"
	MemoryService_QueryMemory_FullMethodName   = "/jennahapi.agent.v1.MemoryService/QueryMemory"
	MemoryService_InspectMemory_FullMethodName = "/jennahapi.agent.v1.MemoryService/InspectMemory"
)

// MemoryServiceClient is the client API for MemoryService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// MemoryService is the unified memory-data plane for an agent workspace.
type MemoryServiceClient interface {
	// Applies a multi-section memory step in a SINGLE Spanner read-write
	// transaction: an optional execution-log step, an optional set of vector
	// chunks, and an optional set of graph writes. The commit is all-or-nothing —
	// if any section fails (e.g. a vector whose width does not match the
	// database), no section's rows are written. The commit is rejected, writing
	// nothing, if the target workspace does not exist under the caller's tenant.
	// Returns a receipt: the commit timestamp and per-memory-type row counts.
	CommitMemory(ctx context.Context, in *CommitMemoryRequest, opts ...grpc.CallOption) (*CommitMemoryResponse, error)
	// Evaluates a multi-section read against ONE read snapshot so results are
	// internally consistent across memory types: an optional semantic (vector)
	// section, an optional graph (GQL) section, and an optional execution-log
	// section. Returns one result section per requested section, plus a fused
	// result when `link` is set. An optional `as_of` timestamp is applied
	// uniformly to every section so all sections observe the same historical
	// instant. Every section is clamped to the caller's (EnterpriseId,
	// AgentInstanceId) slice.
	QueryMemory(ctx context.Context, in *QueryMemoryRequest, opts ...grpc.CallOption) (*QueryMemoryResponse, error)
	// Lists the stored rows of one or more memory sections WITHOUT a query anchor,
	// for debugging and inspection: the vector chunks (content only — embedding
	// vectors are never returned), the graph nodes and edges, and the recent
	// execution-log steps. Unlike QueryMemory it takes no semantic query or graph
	// start pattern; each requested section is simply enumerated within the
	// caller's (EnterpriseId, AgentInstanceId) slice, up to a per-section limit,
	// at ONE read snapshot so every section observes the same instant. This is a
	// read-only diagnostic surface, not a retrieval path.
	InspectMemory(ctx context.Context, in *InspectMemoryRequest, opts ...grpc.CallOption) (*InspectMemoryResponse, error)
}

type memoryServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewMemoryServiceClient(cc grpc.ClientConnInterface) MemoryServiceClient {
	return &memoryServiceClient{cc}
}

func (c *memoryServiceClient) CommitMemory(ctx context.Context, in *CommitMemoryRequest, opts ...grpc.CallOption) (*CommitMemoryResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CommitMemoryResponse)
	err := c.cc.Invoke(ctx, MemoryService_CommitMemory_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *memoryServiceClient) QueryMemory(ctx context.Context, in *QueryMemoryRequest, opts ...grpc.CallOption) (*QueryMemoryResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(QueryMemoryResponse)
	err := c.cc.Invoke(ctx, MemoryService_QueryMemory_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *memoryServiceClient) InspectMemory(ctx context.Context, in *InspectMemoryRequest, opts ...grpc.CallOption) (*InspectMemoryResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(InspectMemoryResponse)
	err := c.cc.Invoke(ctx, MemoryService_InspectMemory_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// MemoryServiceServer is the server API for MemoryService service.
// All implementations must embed UnimplementedMemoryServiceServer
// for forward compatibility.
//
// MemoryService is the unified memory-data plane for an agent workspace.
type MemoryServiceServer interface {
	// Applies a multi-section memory step in a SINGLE Spanner read-write
	// transaction: an optional execution-log step, an optional set of vector
	// chunks, and an optional set of graph writes. The commit is all-or-nothing —
	// if any section fails (e.g. a vector whose width does not match the
	// database), no section's rows are written. The commit is rejected, writing
	// nothing, if the target workspace does not exist under the caller's tenant.
	// Returns a receipt: the commit timestamp and per-memory-type row counts.
	CommitMemory(context.Context, *CommitMemoryRequest) (*CommitMemoryResponse, error)
	// Evaluates a multi-section read against ONE read snapshot so results are
	// internally consistent across memory types: an optional semantic (vector)
	// section, an optional graph (GQL) section, and an optional execution-log
	// section. Returns one result section per requested section, plus a fused
	// result when `link` is set. An optional `as_of` timestamp is applied
	// uniformly to every section so all sections observe the same historical
	// instant. Every section is clamped to the caller's (EnterpriseId,
	// AgentInstanceId) slice.
	QueryMemory(context.Context, *QueryMemoryRequest) (*QueryMemoryResponse, error)
	// Lists the stored rows of one or more memory sections WITHOUT a query anchor,
	// for debugging and inspection: the vector chunks (content only — embedding
	// vectors are never returned), the graph nodes and edges, and the recent
	// execution-log steps. Unlike QueryMemory it takes no semantic query or graph
	// start pattern; each requested section is simply enumerated within the
	// caller's (EnterpriseId, AgentInstanceId) slice, up to a per-section limit,
	// at ONE read snapshot so every section observes the same instant. This is a
	// read-only diagnostic surface, not a retrieval path.
	InspectMemory(context.Context, *InspectMemoryRequest) (*InspectMemoryResponse, error)
	mustEmbedUnimplementedMemoryServiceServer()
}

// UnimplementedMemoryServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedMemoryServiceServer struct{}

func (UnimplementedMemoryServiceServer) CommitMemory(context.Context, *CommitMemoryRequest) (*CommitMemoryResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method CommitMemory not implemented")
}
func (UnimplementedMemoryServiceServer) QueryMemory(context.Context, *QueryMemoryRequest) (*QueryMemoryResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method QueryMemory not implemented")
}
func (UnimplementedMemoryServiceServer) InspectMemory(context.Context, *InspectMemoryRequest) (*InspectMemoryResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method InspectMemory not implemented")
}
func (UnimplementedMemoryServiceServer) mustEmbedUnimplementedMemoryServiceServer() {}
func (UnimplementedMemoryServiceServer) testEmbeddedByValue()                       {}

// UnsafeMemoryServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to MemoryServiceServer will
// result in compilation errors.
type UnsafeMemoryServiceServer interface {
	mustEmbedUnimplementedMemoryServiceServer()
}

func RegisterMemoryServiceServer(s grpc.ServiceRegistrar, srv MemoryServiceServer) {
	// If the following call panics, it indicates UnimplementedMemoryServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&MemoryService_ServiceDesc, srv)
}

func _MemoryService_CommitMemory_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CommitMemoryRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(MemoryServiceServer).CommitMemory(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: MemoryService_CommitMemory_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(MemoryServiceServer).CommitMemory(ctx, req.(*CommitMemoryRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _MemoryService_QueryMemory_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(QueryMemoryRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(MemoryServiceServer).QueryMemory(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: MemoryService_QueryMemory_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(MemoryServiceServer).QueryMemory(ctx, req.(*QueryMemoryRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _MemoryService_InspectMemory_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(InspectMemoryRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(MemoryServiceServer).InspectMemory(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: MemoryService_InspectMemory_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(MemoryServiceServer).InspectMemory(ctx, req.(*InspectMemoryRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// MemoryService_ServiceDesc is the grpc.ServiceDesc for MemoryService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var MemoryService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "jennahapi.agent.v1.MemoryService",
	HandlerType: (*MemoryServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "CommitMemory",
			Handler:    _MemoryService_CommitMemory_Handler,
		},
		{
			MethodName: "QueryMemory",
			Handler:    _MemoryService_QueryMemory_Handler,
		},
		{
			MethodName: "InspectMemory",
			Handler:    _MemoryService_InspectMemory_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "jennah/agent/v1/memory.proto",
}
