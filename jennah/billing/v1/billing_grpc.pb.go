// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: jennah/billing/v1/billing.proto

package billing

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	BillingService_GetBillingState_FullMethodName                = "/jennahapi.billing.v1.BillingService/GetBillingState"
	BillingService_BindMarketplaceRegistration_FullMethodName    = "/jennahapi.billing.v1.BillingService/BindMarketplaceRegistration"
	BillingService_ResolveMarketplaceRegistration_FullMethodName = "/jennahapi.billing.v1.BillingService/ResolveMarketplaceRegistration"
)

// BillingServiceClient is the client API for BillingService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// BillingService is the provider-neutral billing surface for an enterprise.
type BillingServiceClient interface {
	// Reads the caller's active enterprise billing state: its effective tier, which
	// source owns that tier, and every subscription bound to it (plural because
	// one AWS account can hold several concurrent agreements for the same
	// product). External (gateway) RPC. Authenticated.
	//
	// It stays callable when the caller's tier is unusable (an expired trial or a
	// cancelled subscription): it is on the entitlement interceptor's exempt
	// allowlist, because a blocked customer must be able to reach the state that
	// explains how to recover.
	//
	// When `externally_managed` is true the client MUST NOT offer an in-app plan
	// change. Send the user to `manage_url` instead.
	GetBillingState(ctx context.Context, in *GetBillingStateRequest, opts ...grpc.CallOption) (*GetBillingStateResponse, error)
	// Binds the subscription behind a single-use registration handle to the caller's
	// active enterprise and applies its resolved tier. External (gateway) RPC.
	// Authenticated AND gated to ROLE_ROOT/ROLE_ADMIN on the active enterprise,
	// resolved live from the Memberships table. Also on the entitlement exempt
	// allowlist, since this is the call that restores a blocked caller's access.
	//
	// The handle is a bearer capability with a deliberately small blast radius:
	// whoever presents it attaches that subscription to THEIR active enterprise.
	//
	// Binding the same subscription to the same enterprise again is idempotent
	// success: buyers re-enter through AWS's "Set up your account" button routinely.
	//
	// Rejections. Each carries a `google.rpc.ErrorInfo` whose `domain` is
	// "jennah.alphaus.cloud" and whose `reason` is the constant named below. Key on
	// the reason, never on the human-readable message: each reason needs a
	// different recovery path in the UI.
	//
	//	FAILED_PRECONDITION / REGISTRATION_HANDLE_INVALID: expired, already
	//	  consumed, or unknown. Not distinguished from one another on purpose, so a
	//	  caller cannot probe which handles exist.
	//	ALREADY_EXISTS / SUBSCRIPTION_BOUND_ELSEWHERE: bound to a different
	//	  enterprise. The existing binding is NOT moved; recovery is an operator
	//	  action with a recorded reason.
	//	PERMISSION_DENIED / ENTERPRISE_ADMIN_REQUIRED: the caller is a member
	//	  without an administrator role on the active enterprise.
	//	FAILED_PRECONDITION / BILLING_SOURCE_CONFLICT: the enterprise's tier is
	//	  already owned by a DIFFERENT billing source. (A second subscription from
	//	  the SAME source is accepted, not refused.)
	BindMarketplaceRegistration(ctx context.Context, in *BindMarketplaceRegistrationRequest, opts ...grpc.CallOption) (*BindMarketplaceRegistrationResponse, error)
	// Exchanges an AWS Marketplace registration token for the buyer's marketplace
	// identity, persists the subscription in the unbound state, mints a single-use
	// binding handle, and returns the console URL to redirect the browser to.
	//
	// Unauthenticated (interceptor allowlist), because the caller is AWS rather than
	// a jennah user: AWS POSTs the buyer's browser to the public registration
	// endpoint with no cookie and no bearer credential, and the buyer may have no
	// jennah account at all. Exemption from bearer-token auth is not exemption from
	// authentication: this method authenticates its caller by exchanging the
	// provider-issued token with AWS, and rejects the request when that fails.
	//
	// The resolve-and-persist-before-login ordering is the point: the token is
	// short-lived and cannot survive an interactive signup, and the buyer is billed
	// from contract creation whether or not they ever finish registering. After this
	// returns, every remaining step is retryable and nothing is time-critical.
	//
	// Rejections, split so the edge can tell a retry from a dead end. Those naming
	// a reason below carry a `google.rpc.ErrorInfo` whose `domain` is
	// "jennah.alphaus.cloud" and whose `reason` is that constant; key on the
	// reason, never on the human-readable message. UNAVAILABLE and UNIMPLEMENTED
	// carry NO ErrorInfo; for those the status code is the whole signal.
	//
	//	INVALID_ARGUMENT / REGISTRATION_TOKEN_INVALID: missing or malformed token,
	//	  or one AWS refuses. Permanent; do not retry.
	//	FAILED_PRECONDITION / PRODUCT_CODE_NOT_CONFIGURED: the token resolved to a
	//	  product code this deployment has no mapping for. Permanent, and no
	//	  subscription record is written.
	//	UNAVAILABLE: AWS was unreachable or throttled. Retryable, and no handle is
	//	  minted and no partial subscription is left behind. The purchase still
	//	  reaches us independently as a marketplace notification, so no revenue is
	//	  lost by a failed registration.
	//	UNIMPLEMENTED: the deployment has no billing configuration at all.
	ResolveMarketplaceRegistration(ctx context.Context, in *ResolveMarketplaceRegistrationRequest, opts ...grpc.CallOption) (*ResolveMarketplaceRegistrationResponse, error)
}

type billingServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewBillingServiceClient(cc grpc.ClientConnInterface) BillingServiceClient {
	return &billingServiceClient{cc}
}

func (c *billingServiceClient) GetBillingState(ctx context.Context, in *GetBillingStateRequest, opts ...grpc.CallOption) (*GetBillingStateResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetBillingStateResponse)
	err := c.cc.Invoke(ctx, BillingService_GetBillingState_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *billingServiceClient) BindMarketplaceRegistration(ctx context.Context, in *BindMarketplaceRegistrationRequest, opts ...grpc.CallOption) (*BindMarketplaceRegistrationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(BindMarketplaceRegistrationResponse)
	err := c.cc.Invoke(ctx, BillingService_BindMarketplaceRegistration_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *billingServiceClient) ResolveMarketplaceRegistration(ctx context.Context, in *ResolveMarketplaceRegistrationRequest, opts ...grpc.CallOption) (*ResolveMarketplaceRegistrationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ResolveMarketplaceRegistrationResponse)
	err := c.cc.Invoke(ctx, BillingService_ResolveMarketplaceRegistration_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// BillingServiceServer is the server API for BillingService service.
// All implementations must embed UnimplementedBillingServiceServer
// for forward compatibility.
//
// BillingService is the provider-neutral billing surface for an enterprise.
type BillingServiceServer interface {
	// Reads the caller's active enterprise billing state: its effective tier, which
	// source owns that tier, and every subscription bound to it (plural because
	// one AWS account can hold several concurrent agreements for the same
	// product). External (gateway) RPC. Authenticated.
	//
	// It stays callable when the caller's tier is unusable (an expired trial or a
	// cancelled subscription): it is on the entitlement interceptor's exempt
	// allowlist, because a blocked customer must be able to reach the state that
	// explains how to recover.
	//
	// When `externally_managed` is true the client MUST NOT offer an in-app plan
	// change. Send the user to `manage_url` instead.
	GetBillingState(context.Context, *GetBillingStateRequest) (*GetBillingStateResponse, error)
	// Binds the subscription behind a single-use registration handle to the caller's
	// active enterprise and applies its resolved tier. External (gateway) RPC.
	// Authenticated AND gated to ROLE_ROOT/ROLE_ADMIN on the active enterprise,
	// resolved live from the Memberships table. Also on the entitlement exempt
	// allowlist, since this is the call that restores a blocked caller's access.
	//
	// The handle is a bearer capability with a deliberately small blast radius:
	// whoever presents it attaches that subscription to THEIR active enterprise.
	//
	// Binding the same subscription to the same enterprise again is idempotent
	// success: buyers re-enter through AWS's "Set up your account" button routinely.
	//
	// Rejections. Each carries a `google.rpc.ErrorInfo` whose `domain` is
	// "jennah.alphaus.cloud" and whose `reason` is the constant named below. Key on
	// the reason, never on the human-readable message: each reason needs a
	// different recovery path in the UI.
	//
	//	FAILED_PRECONDITION / REGISTRATION_HANDLE_INVALID: expired, already
	//	  consumed, or unknown. Not distinguished from one another on purpose, so a
	//	  caller cannot probe which handles exist.
	//	ALREADY_EXISTS / SUBSCRIPTION_BOUND_ELSEWHERE: bound to a different
	//	  enterprise. The existing binding is NOT moved; recovery is an operator
	//	  action with a recorded reason.
	//	PERMISSION_DENIED / ENTERPRISE_ADMIN_REQUIRED: the caller is a member
	//	  without an administrator role on the active enterprise.
	//	FAILED_PRECONDITION / BILLING_SOURCE_CONFLICT: the enterprise's tier is
	//	  already owned by a DIFFERENT billing source. (A second subscription from
	//	  the SAME source is accepted, not refused.)
	BindMarketplaceRegistration(context.Context, *BindMarketplaceRegistrationRequest) (*BindMarketplaceRegistrationResponse, error)
	// Exchanges an AWS Marketplace registration token for the buyer's marketplace
	// identity, persists the subscription in the unbound state, mints a single-use
	// binding handle, and returns the console URL to redirect the browser to.
	//
	// Unauthenticated (interceptor allowlist), because the caller is AWS rather than
	// a jennah user: AWS POSTs the buyer's browser to the public registration
	// endpoint with no cookie and no bearer credential, and the buyer may have no
	// jennah account at all. Exemption from bearer-token auth is not exemption from
	// authentication: this method authenticates its caller by exchanging the
	// provider-issued token with AWS, and rejects the request when that fails.
	//
	// The resolve-and-persist-before-login ordering is the point: the token is
	// short-lived and cannot survive an interactive signup, and the buyer is billed
	// from contract creation whether or not they ever finish registering. After this
	// returns, every remaining step is retryable and nothing is time-critical.
	//
	// Rejections, split so the edge can tell a retry from a dead end. Those naming
	// a reason below carry a `google.rpc.ErrorInfo` whose `domain` is
	// "jennah.alphaus.cloud" and whose `reason` is that constant; key on the
	// reason, never on the human-readable message. UNAVAILABLE and UNIMPLEMENTED
	// carry NO ErrorInfo; for those the status code is the whole signal.
	//
	//	INVALID_ARGUMENT / REGISTRATION_TOKEN_INVALID: missing or malformed token,
	//	  or one AWS refuses. Permanent; do not retry.
	//	FAILED_PRECONDITION / PRODUCT_CODE_NOT_CONFIGURED: the token resolved to a
	//	  product code this deployment has no mapping for. Permanent, and no
	//	  subscription record is written.
	//	UNAVAILABLE: AWS was unreachable or throttled. Retryable, and no handle is
	//	  minted and no partial subscription is left behind. The purchase still
	//	  reaches us independently as a marketplace notification, so no revenue is
	//	  lost by a failed registration.
	//	UNIMPLEMENTED: the deployment has no billing configuration at all.
	ResolveMarketplaceRegistration(context.Context, *ResolveMarketplaceRegistrationRequest) (*ResolveMarketplaceRegistrationResponse, error)
	mustEmbedUnimplementedBillingServiceServer()
}

// UnimplementedBillingServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedBillingServiceServer struct{}

func (UnimplementedBillingServiceServer) GetBillingState(context.Context, *GetBillingStateRequest) (*GetBillingStateResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetBillingState not implemented")
}
func (UnimplementedBillingServiceServer) BindMarketplaceRegistration(context.Context, *BindMarketplaceRegistrationRequest) (*BindMarketplaceRegistrationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method BindMarketplaceRegistration not implemented")
}
func (UnimplementedBillingServiceServer) ResolveMarketplaceRegistration(context.Context, *ResolveMarketplaceRegistrationRequest) (*ResolveMarketplaceRegistrationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ResolveMarketplaceRegistration not implemented")
}
func (UnimplementedBillingServiceServer) mustEmbedUnimplementedBillingServiceServer() {}
func (UnimplementedBillingServiceServer) testEmbeddedByValue()                        {}

// UnsafeBillingServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to BillingServiceServer will
// result in compilation errors.
type UnsafeBillingServiceServer interface {
	mustEmbedUnimplementedBillingServiceServer()
}

func RegisterBillingServiceServer(s grpc.ServiceRegistrar, srv BillingServiceServer) {
	// If the following call panics, it indicates UnimplementedBillingServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&BillingService_ServiceDesc, srv)
}

func _BillingService_GetBillingState_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetBillingStateRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(BillingServiceServer).GetBillingState(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: BillingService_GetBillingState_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(BillingServiceServer).GetBillingState(ctx, req.(*GetBillingStateRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _BillingService_BindMarketplaceRegistration_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(BindMarketplaceRegistrationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(BillingServiceServer).BindMarketplaceRegistration(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: BillingService_BindMarketplaceRegistration_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(BillingServiceServer).BindMarketplaceRegistration(ctx, req.(*BindMarketplaceRegistrationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _BillingService_ResolveMarketplaceRegistration_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ResolveMarketplaceRegistrationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(BillingServiceServer).ResolveMarketplaceRegistration(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: BillingService_ResolveMarketplaceRegistration_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(BillingServiceServer).ResolveMarketplaceRegistration(ctx, req.(*ResolveMarketplaceRegistrationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// BillingService_ServiceDesc is the grpc.ServiceDesc for BillingService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var BillingService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "jennahapi.billing.v1.BillingService",
	HandlerType: (*BillingServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "GetBillingState",
			Handler:    _BillingService_GetBillingState_Handler,
		},
		{
			MethodName: "BindMarketplaceRegistration",
			Handler:    _BillingService_BindMarketplaceRegistration_Handler,
		},
		{
			MethodName: "ResolveMarketplaceRegistration",
			Handler:    _BillingService_ResolveMarketplaceRegistration_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "jennah/billing/v1/billing.proto",
}
