// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: jennah/datastore/v1/schema.proto

package datastore

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SchemaService_DeclareTables_FullMethodName = "/jennahapi.datastore.v1.SchemaService/DeclareTables"
	SchemaService_GetSchema_FullMethodName     = "/jennahapi.datastore.v1.SchemaService/GetSchema"
)

// SchemaServiceClient is the client API for SchemaService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SchemaService is the tenant-facing schema catalog for a dataset's
// user-defined tables.
//
// Tenants declare LOGICAL tables and columns. The control plane validates each
// declaration against a strict grammar, mints a PHYSICAL identifier for every
// object, records the logical->physical mapping in the catalog, and issues real
// Spanner DDL. Callers only ever name logical identifiers; a caller string never
// reaches query text. A logical name absent from the dataset's catalog is a
// not-found, never a fallthrough to raw interpolation — which is what makes
// identifier injection structurally impossible rather than merely filtered.
//
// Every table's primary key is led by `(EnterpriseId, DatasetId)`, prepended by
// the platform and NOT expressible in a declaration. That is what makes a
// dataset's rows a contiguous key range and isolation a mandatory bound
// predicate rather than a remembered WHERE clause.
type SchemaServiceClient interface {
	// Declares or evolves the dataset's tables. Idempotent and ADDITIVE:
	// re-declaring an existing table or column is a no-op, and a new column is
	// added as an online change. Destructive changes (dropping a table or column,
	// or retyping one) are REJECTED in this version — they can rewrite or lose
	// live tenant data, and deserve an explicit, separately-authorized migration
	// path rather than being reachable from the ordinary declare call.
	//
	// Validation happens entirely before any DDL is issued, so a rejected
	// declaration leaves the schema untouched — including when it is refused for
	// exceeding a dataset's table, column, or index ceiling, which returns
	// RESOURCE_EXHAUSTED and never a raw backend error.
	//
	// Spanner applies schema changes asynchronously, so an accepted declaration
	// returns with each table at SCHEMA_STATUS_PENDING; poll GetSchema until every
	// table reaches SCHEMA_STATUS_READY before committing data to it.
	//
	// Requires datastore.schema:manage, which is deliberately NOT part of the
	// built-in member role: declaring a table issues DDL against a shared
	// data-plane database, so it is always an explicit grant to a custom role or
	// an explicitly scoped key. It IS grantable to an API key — a machine
	// principal building an app has to be able to declare its own tables.
	DeclareTables(ctx context.Context, in *DeclareTablesRequest, opts ...grpc.CallOption) (*DeclareTablesResponse, error)
	// Reads the dataset's logical schema: every declared table with its columns,
	// indexes, vector modality, and current apply status, plus usage against the
	// tier's ceilings. This is the poll surface for a declaration still applying.
	// Requires datastore.schema:read.
	GetSchema(ctx context.Context, in *GetSchemaRequest, opts ...grpc.CallOption) (*GetSchemaResponse, error)
}

type schemaServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewSchemaServiceClient(cc grpc.ClientConnInterface) SchemaServiceClient {
	return &schemaServiceClient{cc}
}

func (c *schemaServiceClient) DeclareTables(ctx context.Context, in *DeclareTablesRequest, opts ...grpc.CallOption) (*DeclareTablesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DeclareTablesResponse)
	err := c.cc.Invoke(ctx, SchemaService_DeclareTables_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *schemaServiceClient) GetSchema(ctx context.Context, in *GetSchemaRequest, opts ...grpc.CallOption) (*GetSchemaResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetSchemaResponse)
	err := c.cc.Invoke(ctx, SchemaService_GetSchema_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// SchemaServiceServer is the server API for SchemaService service.
// All implementations must embed UnimplementedSchemaServiceServer
// for forward compatibility.
//
// SchemaService is the tenant-facing schema catalog for a dataset's
// user-defined tables.
//
// Tenants declare LOGICAL tables and columns. The control plane validates each
// declaration against a strict grammar, mints a PHYSICAL identifier for every
// object, records the logical->physical mapping in the catalog, and issues real
// Spanner DDL. Callers only ever name logical identifiers; a caller string never
// reaches query text. A logical name absent from the dataset's catalog is a
// not-found, never a fallthrough to raw interpolation — which is what makes
// identifier injection structurally impossible rather than merely filtered.
//
// Every table's primary key is led by `(EnterpriseId, DatasetId)`, prepended by
// the platform and NOT expressible in a declaration. That is what makes a
// dataset's rows a contiguous key range and isolation a mandatory bound
// predicate rather than a remembered WHERE clause.
type SchemaServiceServer interface {
	// Declares or evolves the dataset's tables. Idempotent and ADDITIVE:
	// re-declaring an existing table or column is a no-op, and a new column is
	// added as an online change. Destructive changes (dropping a table or column,
	// or retyping one) are REJECTED in this version — they can rewrite or lose
	// live tenant data, and deserve an explicit, separately-authorized migration
	// path rather than being reachable from the ordinary declare call.
	//
	// Validation happens entirely before any DDL is issued, so a rejected
	// declaration leaves the schema untouched — including when it is refused for
	// exceeding a dataset's table, column, or index ceiling, which returns
	// RESOURCE_EXHAUSTED and never a raw backend error.
	//
	// Spanner applies schema changes asynchronously, so an accepted declaration
	// returns with each table at SCHEMA_STATUS_PENDING; poll GetSchema until every
	// table reaches SCHEMA_STATUS_READY before committing data to it.
	//
	// Requires datastore.schema:manage, which is deliberately NOT part of the
	// built-in member role: declaring a table issues DDL against a shared
	// data-plane database, so it is always an explicit grant to a custom role or
	// an explicitly scoped key. It IS grantable to an API key — a machine
	// principal building an app has to be able to declare its own tables.
	DeclareTables(context.Context, *DeclareTablesRequest) (*DeclareTablesResponse, error)
	// Reads the dataset's logical schema: every declared table with its columns,
	// indexes, vector modality, and current apply status, plus usage against the
	// tier's ceilings. This is the poll surface for a declaration still applying.
	// Requires datastore.schema:read.
	GetSchema(context.Context, *GetSchemaRequest) (*GetSchemaResponse, error)
	mustEmbedUnimplementedSchemaServiceServer()
}

// UnimplementedSchemaServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSchemaServiceServer struct{}

func (UnimplementedSchemaServiceServer) DeclareTables(context.Context, *DeclareTablesRequest) (*DeclareTablesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method DeclareTables not implemented")
}
func (UnimplementedSchemaServiceServer) GetSchema(context.Context, *GetSchemaRequest) (*GetSchemaResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetSchema not implemented")
}
func (UnimplementedSchemaServiceServer) mustEmbedUnimplementedSchemaServiceServer() {}
func (UnimplementedSchemaServiceServer) testEmbeddedByValue()                       {}

// UnsafeSchemaServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SchemaServiceServer will
// result in compilation errors.
type UnsafeSchemaServiceServer interface {
	mustEmbedUnimplementedSchemaServiceServer()
}

func RegisterSchemaServiceServer(s grpc.ServiceRegistrar, srv SchemaServiceServer) {
	// If the following call panics, it indicates UnimplementedSchemaServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SchemaService_ServiceDesc, srv)
}

func _SchemaService_DeclareTables_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DeclareTablesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SchemaServiceServer).DeclareTables(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SchemaService_DeclareTables_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SchemaServiceServer).DeclareTables(ctx, req.(*DeclareTablesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _SchemaService_GetSchema_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetSchemaRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SchemaServiceServer).GetSchema(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SchemaService_GetSchema_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SchemaServiceServer).GetSchema(ctx, req.(*GetSchemaRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// SchemaService_ServiceDesc is the grpc.ServiceDesc for SchemaService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SchemaService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "jennahapi.datastore.v1.SchemaService",
	HandlerType: (*SchemaServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "DeclareTables",
			Handler:    _SchemaService_DeclareTables_Handler,
		},
		{
			MethodName: "GetSchema",
			Handler:    _SchemaService_GetSchema_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "jennah/datastore/v1/schema.proto",
}
